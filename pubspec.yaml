name: which_app
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  percent_indicator: ^2.1.7+2
  shared_preferences: ^2.0.6
  firebase_core: ^0.7.0
  cloud_firestore: ^0.16.0+1
  fluttertoast: ^8.0.7
  uuid: 3.0.4
  flutter_native_splash: ^1.2.0
  provider: ^4.0.2
  flutter_colorpicker: ^0.5.0

flutter_native_splash:
  image: "images/sweets-vs-fruits.png"
  color: "#c9e8f5"

dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:

  uses-material-design: true
